import { type Id, type Op } from '@graphprotocol/grc-20';
import type { Entity } from '@graphprotocol/hypergraph';
export type PreparePublishParams<S extends Entity.AnyNoContext> = {
    entity: Entity.Entity<S>;
    publicSpace: string | Id.Id;
};
export declare const preparePublish: <S extends Entity.AnyNoContext>({ entity, publicSpace, }: PreparePublishParams<S>) => Promise<{
    ops: Op[];
}>;
//# sourceMappingURL=prepare-publish.d.ts.map