import type { Op } from '@graphprotocol/grc-20';
import { Connect } from '@graphprotocol/hypergraph';
import type { Hash } from 'viem';
type PublishParams = {
    name: string;
    ops: Op[];
    walletClient: Connect.SmartSessionClient;
    space: string;
};
type PublishResult = {
    txResult: Hash;
    to: Hash;
    data: Hash;
    cid: string;
};
export declare const publishOps: ({ name, ops, walletClient, space }: PublishParams) => Promise<PublishResult>;
export {};
//# sourceMappingURL=publish-ops.d.ts.map