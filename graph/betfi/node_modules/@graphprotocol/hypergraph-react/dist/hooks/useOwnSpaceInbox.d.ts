import { type Inboxes } from '@graphprotocol/hypergraph';
/**
 * Hook for managing a user's own space inbox
 * Provides full read/write capabilities for the user's own space inbox
 */
export declare function useOwnSpaceInbox({ spaceId, inboxId, autoCreate, isPublic, authPolicy, }: {
    spaceId: string;
    inboxId?: string;
    autoCreate?: boolean;
    isPublic?: boolean;
    authPolicy?: Inboxes.InboxSenderAuthPolicy;
}): {
    messages: import("@graphprotocol/hypergraph").InboxMessageStorageEntry[];
    loading: boolean;
    error: Error | null;
    refresh: () => Promise<void>;
    sendMessage: (message: string) => Promise<void>;
    inboxId: string | undefined;
    isPublic: boolean;
    authPolicy: string;
    encryptionPublicKey: string;
};
//# sourceMappingURL=useOwnSpaceInbox.d.ts.map