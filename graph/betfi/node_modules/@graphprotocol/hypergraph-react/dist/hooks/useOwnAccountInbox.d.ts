/**
 * Hook for managing a user's own inbox
 * Provides full read/write capabilities for the user's own inbox
 */
export declare function useOwnAccountInbox(inboxId: string): {
    messages: import("@graphprotocol/hypergraph").InboxMessageStorageEntry[];
    loading: boolean;
    error: Error | null;
    refresh: () => Promise<void>;
    sendMessage: (message: string) => Promise<void>;
    isPublic: boolean;
    authPolicy: string;
    encryptionPublicKey: string;
};
//# sourceMappingURL=useOwnAccountInbox.d.ts.map