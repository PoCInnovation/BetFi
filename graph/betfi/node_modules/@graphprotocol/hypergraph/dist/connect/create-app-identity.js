import { generatePrivateKey, privateKeyToAccount } from 'viem/accounts';
import { createIdentityKeys } from './create-identity-keys.js';
export const createAppIdentity = () => {
  const keys = createIdentityKeys();
  const addressPrivateKey = generatePrivateKey();
  const {
    address
  } = privateKeyToAccount(addressPrivateKey);
  return {
    ...keys,
    address,
    addressPrivateKey
  };
};
//# sourceMappingURL=create-app-identity.js.map