import type { DocHandle } from '@automerge/automerge-repo';
import * as Schema from 'effect/Schema';
import type { AnyNoContext, DocumentContent, Entity } from './types.js';
/**
 * Find the entity of the given type, with the given id, from the repo.
 */
export declare const findOne: <const S extends AnyNoContext>(handle: DocHandle<DocumentContent>, type: S, include?: { [K in keyof Schema.Schema.Type<S>]?: Record<string, never>; }) => (id: string) => Entity<S> | undefined;
//# sourceMappingURL=findOne.d.ts.map