import { cryptoBoxEasy, cryptoBoxKeyPair, cryptoBoxOpenEasy } from '@serenity-kit/noble-sodium';
export function generateKeypair() {
  const {
    publicKey,
    privateKey
  } = cryptoBoxKeyPair();
  return {
    publicKey,
    secretKey: privateKey
  };
}
export function encryptKeyBox({
  message,
  publicKey,
  secretKey,
  nonce
}) {
  return cryptoBoxEasy({
    message,
    publicKey,
    privateKey: secretKey,
    nonce
  });
}
export function decryptKeyBox({
  ciphertext,
  nonce,
  publicKey,
  secretKey
}) {
  return cryptoBoxOpenEasy({
    ciphertext,
    publicKey,
    privateKey: secretKey,
    nonce
  });
}
//# sourceMappingURL=key-box.js.map