import { decryptKeyBox } from './key-box.js';
export function decryptKey({
  privateKey,
  publicKey,
  keyBoxNonce,
  keyBoxCiphertext
}) {
  const key = decryptKeyBox({
    nonce: keyBoxNonce,
    ciphertext: keyBoxCiphertext,
    publicKey,
    secretKey: privateKey
  });
  return key;
}
//# sourceMappingURL=decrypt-key.js.map